General files to start reading for better context:
- [README.md](README.md)
- [AGENTS.md](AGENTS.md)
This app works fine. I created this app to help me find jobs in Upwork since I am a very busy freelancer. It is a personal app, just for me and I don't plan to public this (save that in your agent file).
Currently, It basically just sees the most new jobs that exist within the filters that I specified using a APIFY API that I finded that brings jobs of upwork.

I want to enhance this little project a lot, it will have a hole new AI integration that you will build from scratch. So this will be a quite tuff task and I need you to ultrathink and don't rush it, you should think a LOT in each step and break it into smaller tasks so you don't lose any point that I will talk about in this prompt. By the way, if you think that spliting multiple agents to do certain tasks like research/discovery, allways use the most flagship model (GPT 5.4) possible in those instances and ALLWAYS use the highest thinking possible (nothing of mini models or low thinking, the thinking should be xhigh – like extra high).
Handle those sub agents gracefully so you will spend less of you context tasks with not core tasks like investigation or web research.

Now, I will start to describe all the task's I want you to do.
Well, a general overview is: I want to use AI to, based on a knowlage that I will input, to generate a proposal cover letter for the the jobs that are of my interest – and the logic of deciding what job is relevant for me will also be based in ai. And I want to use my open ai subscription to do that so I don't need to waste money with API credits since I already pay a subscription.

Now I will breakdown this.

First, let's re-think te strucutre of the job model in the serverpod server. The [job_info.spy.yaml](pascoa_scout_server/lib/src/entities/upwork_scrap/job_info.spy.yaml) should be a model. With a table parameter, so do this. It will have a relationship with the `JobAnalysisState` ( [job_analysis_state.spy.yaml](pascoa_scout_server/lib/src/entities/job_analysis_state.spy.yaml) ) class that I just created. I created the `JobAnalysisState` and several other models such as `JobProposalAnswerToQuestion`, `JobScore`, `JobProposal` and  `JobAnalysisState` that you will modify in order to make them table and make the variables as relationships like I will explain throught this paragraph.
This class will handle the state of the job application. It will have all the data of the state of the ai process's that will be done on top of the job. The relation that this model will have with the `JobInfo` will be a 1:1 relationship – so it will exist in both files. The moment you create a `JobInfo` in the database, create also a `JobAnalysisState` attached to it.
After you created this, you will later call ai to generate the response for the job questions and the cover leter, and will also use it to create a score. And all of this models will be attached to the `JobAnalysisState` so make them be a relationship 1:1 (where the relationship exists in both sides). Garante all of them have there own table, and also garantee everything, like the `Question` in [job_proposal_answer_to_question.spy.yaml](pascoa_scout_server/lib/src/entities/job_proposal_answer_to_question.spy.yaml) is also a relationship with the questions "spy.yaml" model that will also have a table and will also have a relationship with the `JobInfo` (in a relation of 1:n since a job info can have multiple questions but a question is attached only to one job). So garantee all that needs to be table will be and don't forget to include them in response to the app. Don't forget to do any relationship since the scafold, all `JobProposal`, `JobAnalysisState`, `JobInfo`, `Question`, `JobProposalAnswerToQuestion` and `JobScore` should be models and deeply attached with relationships between others (use the `attachRow` to make the relationships, if you have any questions then ask serverpod mcp). Add indexes if needed. 

Another important thing you should do is: make the id of JobInfo renamed to "upworkId" so we do a different id from the database id (that uses id as default mapping) to the one of upwork. And make the upwork one as a index and make it unique and this way make sure to not save in the database the `JobInfo` that already exist, you should call the `.update` function instead of the `.insert` if it already exists (make sure to check in the database if exists). Since now we will save the jobs in the database, let's at least ensu

Now, let's go to the part where we will implement the queue system that will see what are the items that need to receive ai corrections. The architecture will need to take in account that all AI processing will be done by the codex cli since it is already logged in and ready to run. We will run 2 ai processes in parallel at max time.
So, I will breakdown the AI structure I want you to build:

The AI that will be using in codex cli since this way I won't need to pay any aditional value more then what I already paid with my subscription. The cli is already logged in my computer. And since this is a project for me, I will allways run this locally so you don't even need to verify if it is installed since I can garantee it is since it is running in my pc.
There is a package in pubdev called "codex: ^0.104.0" that has this logic done. But I might need to modify something... So I want to clone this project and create my own on top of it, So, you should run "git clone git@github.com:leynier/codex-dart.git" to clone the project, then rename it to "codex_cli_interface" (rename in all parts that have reference to the current name of the package, so if decide to upload it in the future to pub dev it will not return any error complaining that this package already exits). So, with the package cloned in the repository use it as a path parameter in the [pubspec.yaml](pascoa_scout_server/pubspec.yaml) so we can import it. And if you want to do any change in this package in order to make it work if you think modifications are required you can do it since the package is cloned. By the way, remove the git folder/files and any git reference. After you clone the repository also read the readme file.

Now with the "AI usage source" explained/created, the next step is to create a orchestrator for it. All ai iteractions will pass by this class that will controll the ai iteractions in the server. It will be like a controller/facade class. It will have a layer that will controll the concurrency of ai access that should be the max of 2 in parallel (make this a constant). Use the package "executor: ^2.2.5". This orchestrator class will basicly repass all calls to the package but with that layer of concurrency controll (more then 2 it will not throw any error, it will just add it in to the queue – this package does this).

I will not explain a thing that will need to be done in the app before we start the of ai generation content, we will need to do a change in the app.
Currently the app only has basicly one page. And it has a mini onboarding process where, in case there is no previous `JobFilter` in the shared preferences, it will present a user with a UI for the user to fullfill the initial job filter he wants. We will do a similar thing (the logic of "display that form if the user did not inputed this required thing yet") but for texts fields... I want the user to insert a BIG text (like a readme, but it will be a basic textfield box with unlimited height – the textfield does not need to have any readme interpretor) for 3 fields, that will be:
- Curriculum
This will be a digitalized version of the curriculum of the user casted to a string in a readme format. This will give context about ai related to me and what habilities/capacities I have.
- How I like proposals to be answered
I want AI to write proposals in the way that I would tipically write, so I will want to descripe to ai how I like proposals to be wrote and I'll do this in this text.
- What I consider a good job opportunity
It is not all jobs that apear in the site that I think is a good opportunity for me and that it is worth wasting connections (upwork credit system to apply to jobs). The filter helps a lot but still there will be several jobs that are in my style but have potential flags that I want to avoid... So, I want to describe what type of job so later the ai will be able to analyse and give a rank of how much that jobs fits with what I am searching and my habilities.

Ask this for the user if they did not inputed yet in the past (is not in shared preferences). EACH TEXT WILL HAVE ITS UNIQUE SCREEN. If the user fullfilled one text and closed the app, when he re-opens he will need to only fullfill the other 2 since the first is already attached. Each page should have a title, description and a GIANT textfield bellow (with expanded lines to get the most space available) and a button in the button in order to continue. Make sure the max caracters is 60k and the minimum is 100 chars for each field/screen... This will be sent to api that will store them in order to be able to use them in other places (create a different "spy.yaml" model for each one of them)

With that AI structure builted, let's start the layer that will do the ai things. This will not be done in sequence and will be a independent event that will be run by a future call (ask serverpod mcp how future calls work if needed).

You will run 3 Future calls in a cycle. The future call will run and, in the END, AFTER running, will program a new one to run again in 5 minutes – this way making a loop. 
We will run a Future call that will run in loop (one calling the other). It will be tree future calls:
The first future call future call will do a query (that will be eficient because of the indexes that I asked you to add) that will get all `JobAnalysisState` that still DO NOT have a `JobScore` (with a limit of 20 items). Then, you will run with AI for each one of those items (the orchestrator will garantee it only runs 2 per time) you will call ai in order to generate a score and a text that justifies the score – so use schema feature for this, you can see a example of this in the readme of the package you will clone but if needed you can modify anything you want in the pacakge as well. Create a folder to run the ai's for each one of this operations and write the markdown file in that folder with the readme of what I consider a good job AND my curriculum as well and in the prompt you should say the codex to look those files and tag them in the same way the codex cli expect file tag and be very explicit when telling it to not continue without reading that file. Also tell in the prompt that the score (that should be a int) can be between zero and 100.
After that future call ends (wait until the 20 items had ended) it will call the next future call, that will be the one that gets the proposal. You will do a query in this future call that will get the first 20 `JobAnalysisState` THAT STILL DO NOT HAVE `JobProposal`, ordored by the ones with most fit score (please also make a `_CompactFilterRunTab` so the user can configure the minimal percentage in order to trigger ai generation, make it 70% by default but the user can configure withing arange of 0 to 100). So this way you will filter out the ones that are not in that minimal score – make this percentage a index as well since we will be doing queries by it. For each one of those `JobAnalysisState` fetched we will generate a JobProposal with the help of ai. similar to what you did in the previous FutureCall, in this one you will put in the folder where the ai is running the readme of my curriculum AND also the markdown file representing my preferences of how to write proposal. With all this rich context, the ai will be able to generate outstanding proposals.
When, this `FutureCall` with call a third future call. And what will be this future call? It will be basicly the exact same pooling logic that is in the client currently in [job_sync_providers.dart](pascoa_scout_flutter/lib/interactor/job_sync/job_sync_providers.dart) – so it will be moved to api side and will no longer be in the client side. Currently, on the client side, the user has the option to start the polling, and then he can pause at any moment and stop the polling from working. On the client side, we also show how much time is into the next polling and those kinds of things. After it ends this process of polling new jobs, it will call then the first future call and by that and beyound... So the flow is:
Fetch new jobs in upwork -> Categorize by score how much is the fit of that job to what I am looking for -> Create proposals and answers with ai to jobs with high score (default is 70%, but the user can change by a new setting you will do in _CompactFilterRunTab ) -> Fetch new jobs in upwork (again, and by that the loop continues) -> [...]

This behavior will change because now we will show in a loop way. The user still will be able to pause and play, but if he pauses, the only thing that he will do is trigger some place in the API side that will make that the polling will be jumped. It will continue this loop, but when it gets to the part of synchronizing the newest up work things, it will just jump this future call and go directly to the previous one. Now we will not have the timer until the next polling, because we won't be able to know, since we don't know how much time the other future calls will take in order to run. That is a thing that will change. The user will be able to continue to pause and unpause, but he will not be able to see how much time it will take into the next one to run.

By the way, I want to know what is happening so make a constant stream that will be started in the moment the user opens the app and it will stream in what moment of that current loop of 3 futurecall. So it will show if it is Getting more jobs in upwork, Generating compatibility score for the jobs or generating cover letters/answers. Also display how much time it had been in the current flow it is (the time since the datetime it started). If the user had paused the job search, then just make a `await Future.delayed` of 5 minutes until starting the step in the flow...

ALL the numbers I talked here like "20 items per time", should be "configurable" by the user in the _CompactFilterRunTab at [job_scrapper_config_tab.dart](pascoa_scout_flutter/lib/ui/tabs/job_scrapper_config_tab.dart) component by a settings, with the same ui of the pooling interval.

Now, at a brand new section that will need to discuss, sicne it will be needed with this new refactor, it to re-think the [job_listage_tab.dart](pascoa_scout_flutter/lib/ui/tabs/job_listage_tab.dart) 
We will literally make a GIANT refactor in a core way. The will be more good to use.
Now, it will not return dirrectly the `JobInfo` model and instead it will return the `JobAnalysisState` that will use the `include` of serverpod to include everything in it.
The UI will need to be shifted in a way that the the user can see that there is answers.
Also, add in the `JobAnalysisState` a "createdJobInfoAt", a "createdJobScoringAt" and a "createdJobAiResponsesAt" – that will all be DateTime's and besides the "createdJobInfoAt", all of them will be nullable.
I will explain how will be the search area that I want now. Do not use the current approach of a big card that contains everything, I don't like it... Like currently there is a title "Job matches" and a description... those could be directly in the background and not in a card... the only thing in the current ui that makes sense to be in a card is the "saved matches" text. So in this new UI, don't make the same error two times. Only put things in a card if they need to, but don't put one big card that will contain all the other cards. I don't like this UI approach. Okay, like the search bar, the search bar should have a UI of a search card. It should be inside a card, because the text field itself will have a feel to it and will look like a card. That's the thing that is okay, but don't put everything inside a card, okay. 
Then, the ui will be the following: In the top, a search area where the user will be abble to search by name the jobs. In the left of the searchbar, there will be a refresh icon (that will refresh with the current aplied filters, bringing potential new jobs or the new data like score or ai answers that where not yet generated to when it was run) and in the right of that searchbar fill be a filters button that will open a dialog with all filters.
Bellow there will be a other row, this time a row of chips that indicate the current applied filters (can be a `Wrap` widget)...
There will be a filter of "Analysis state" – So the default will be "All" and there will be 2 other options; "With at least score generated" and "With ai answers generated". The "With ai answers generated" by default will also contain everything in group "With at least score generated" because, as I mention previosly, there is a minimum of percentage in order to generate the ai response, so this garantees that only matchs with score will be able to have answers as well. There should also be filters to the other states as well. Make sure to put sliders for range and those kind of things... Put also filters for createdJobInfoAt, createdJobScoringAt, createdJobAiResponsesAt so I can set max old time (like, 4 hours to createdJobInfoAt, for example). See [job_scrapper_config_tab.dart](pascoa_scout_flutter/lib/ui/tabs/job_scrapper_config_tab.dart) and you will find great ideas of possible filters. Make ui of filtering be great. Add a boolean checkbox filter "Has questions" filter as well. 
Make sure the save button only apears when the user had changed the current filter, use hash or something like that to know if it was changed...
In the right of the filter button, The user will see a card that indicates the current order by mecanism and when hovered the user will see new options of order by that will apear in a dropdown menu format...
The user will be able to order by:
- By biggest hourly rate
- By smallest hourly rate
- By biggest fixed price
- By smallest fixed price
- By job relative date (JobInfo.relativeDate)
- By job absolute date (JobInfo.absoluteDate)
- Most recent createdJobInfoAt
- Most recent createdJobScoringAt
- Most recent createdJobAiResponsesAt
- By client rating
- By higest client hire rate (JobInfo.clientHireRatePercent)
- By higest client avg hourly rate (JobInfo.clientAvgHourlyRate)
- By higest client name confidence (JobInfo.clientNameConfidencePercent)
- By higest client rating (JobInfo.clientRating)
- By higest client total spent (JobInfo.clientTotalSpent)
- By higest client (JobInfo.clientHireRatePercent)
Of course, don't write those name on the dropdown because there are variables names, put better dropdown names.

Below this filter will be the pagination itself, so it will have all jobs paginated. 
We would use [pagination_metadata.spy.yaml](pascoa_scout_server/lib/src/entities/others/pagination_metadata.spy.yaml) – that is a model that I created but still is not used. So each page will have the pagination.
The user will see a "load more" button in the end in case there is a next page (the `PaginationMetadata.hasNextPage` is true). On top of that button that will be more clear, will be all the page. You should do something like Google does: it shows page one, two, three, four, five, and those kinds of things. Of course, the total amount of pages that exist will have an arrow on the left and on the right so the user can change between the current selected page.
Put this in the bottom so the user needs to scroll down to see it. The user can go to the next page, or he can jump some amount of pages if he wants. 
The `PaginationMetadata.referencePaginationHourStamp` is a very important variable of this model because it represents the date where the first pagination was done, and this will be used to compare to the variables of (datecreatedJobInfoAt, createdJobScoringAt and createdJobAiResponsesAt) to only guarantee consistency between pagination. Maybe, between the user going to the next page, a new value appears that is within the scope, but if that is the case, then the pagination will be confused. While the user did not click the refresh button, he will be stuck in all the things that were generated before or the time that is specified when he did the first pagination. This way, the user will have consistency between pagination, and we will be able to calculate things like the amount of pages, because otherwise the amount of pages will increase more and more by the time the sync system (the logic of the 3 futurecalls) add new jobs/infos. This will guarantee consistency.
The response will be [job_analysis_pagination.spy.yaml](pascoa_scout_server/lib/src/entities/job_analysis_pagination.spy.yaml) and it will have the data of this page.

Another thing that will need to change is the `_JobCard` in [job_listage_tab.dart](pascoa_scout_flutter/lib/ui/tabs/job_listage_tab.dart). First thing is to make a separated widget for it because currently it is to big to be in the same file of the listage, since that listage file will get bigger as well since there will be all the pagination things going on. After you did that, you will then do another change. For example, as I said previously, we will have a general search in this list stage, right? The user will be able to refresh, and when he refreshes, if previously there was a job card that did not have any AI-generated answer, now it will have so the user can refresh the whole stage. Maybe the user doesn't want to refresh the whole stage; he only wants to refresh a single card in order to see if the answers appeared for that job or not. Maybe he has seen a job; he says, "Oh, this job looks great, and oh, there is no AI-generated answer." For example, there is no AI-generated score index, and he maybe wants to check if this is already generated by the script loop that is running in the background in the server. He will then want to click to refresh only that card. You will create an endpoint that will get the most updated fetched version and will show everything related to that card. It will return the updated version of that card, so only that card will be changed in order to show the answers or the score if there isn't any yet. This button should be in the top right, and when I say that, I am saying that you should make this widget a stack. It should be literally up the borders of the widget, so it should be almost out of the component. It should be really in the top right, and it will only appear if the job state itself is not completed. A job state they already had passed the whole flow and already has a score and the AI answers and cover letter generated will never receive newer data; it already has everything. Because of that, it does not make sense to show this button for those kinds of jobs.

Of course, another thing that you should do in this task is refactor this job card in order to add the descriptions, the AI-generated cover letter, and those kinds of things. The AI-generated cover letter should only show two lines and a button to copy the rest. It should be a selectable text so I can select parts of the text if I want after expanding. The answers should be this: you can show them entirely and with a copy button as well so I can copy them if I want. 

PS: In the end ensure there is no static analysis error in none of the places and that you had run the `serverpod generate` command and that all yamls of the folders have the pub get command working.

PS: Garantee there is no endpoint calling other endpoint, if you want to make a logic shared use a mixin. This way we also dont duplicate logic by that following the DRY principle (don't repeat yourself).