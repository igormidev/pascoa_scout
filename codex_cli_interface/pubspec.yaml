name: codex_cli_interface
description: Dart SDK wrapper for the Codex CLI over JSONL stdin/stdout.
version: 0.104.0

environment:
  sdk: ^3.10.9

resolution: workspace

dependencies:
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
