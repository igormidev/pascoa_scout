name: pascoa_scout_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  dio: ^5.9.2
  result_dart: ^2.2.0
  serverpod: 3.4.2
  serverpod_auth_idp_server: 3.4.2

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.4.2
  test: ^1.25.5

serverpod:
  scripts:
    # Starts the server and applies migrations
    start: dart bin/main.dart --apply-migrations

    # Build the Flutter web app and copy it to the server's web directory.
    # Windows uses xcopy since flutter's --output flag doesn't work there.
    # See: https://github.com/flutter/flutter/issues/157886
    flutter_build:
      windows: >-
        (if exist web\app rmdir /S /Q web\app)
        & cd /D ..\pascoa_scout_flutter
        && flutter build web --base-href /app/ --wasm
        && xcopy /E /I /Y build\web ..\pascoa_scout_server\web\app
      posix: |
        cd ../pascoa_scout_flutter
        flutter build web --base-href /app/ --wasm --output ../pascoa_scout_server/web/app
